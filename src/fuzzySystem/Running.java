package fuzzySystem;

public class Running {

	public main {
		CannonSystem cannon = new CannonSystem(23, 52, 0);
		Integer dx = new Integer(0);
		Integer dy = new Integer(0);

		int cannonAngle = 45;
		int shotSpeed = 30;

		do {
			cannon.shot(cannonAngle, shotSpeed, dx, dy);
			feed fuzz 

		} while (dy.getValue() != 0);
	}
	
}
